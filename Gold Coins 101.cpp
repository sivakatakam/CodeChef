#include<bits/stdc++.h>
using namespace std;
int main()
{
    int q, w, z, x;
    cin >> z >> x >> q >> w;
    if (q > w)
    {
        cout << z;
    }
    else
    {
        cout << x;
    }
}
