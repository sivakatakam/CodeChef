#include<stdio.h>
#include<math.h>
int main()
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
        int n,r;
        scanf("%d",&n);
        r=sqrt(n);
        printf("%d\n",r);
    }
}
